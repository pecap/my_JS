/*
$(function(){
	//alert('hi'); as a check to see that everything is set up properly and we are linked to CDN
	
	// selecting by id
	
	//$('#first').addClass('highlight');
	
	// selecting by element
	
	//$('p').addClass('highlight');
	
	// selecting by class
	
	//$('.chosen').addClass('highlight');
	
	// some more advanced cases
	
	// case 1 - first + chosen 
	//$('#first, .chosen').addClass('highlight');
	
	// case 2 - contains
	
	//$('li:contains("Three")').addClass('highlight');
	
	// case 3 - next, prev
	
	//$('li:contains("Three")').next().addClass('highlight');
	//$('li:contains("Three")').prev().addClass('highlight');
	
	// case 4 - siblings, parent
	
	//$('li:contains("Three")').siblings().addClass('highlight');
	//$('li:contains("Three")').parent().addClass('highlight');
	
	// case 5 - individual children of items
	//looking for a first child in the list and add highlight
	//$('li:nth-child(1)').addClass('highlight');
	
	// selecting by attributes
	
	// case 1 - looking for a certain name
	//$('p[name="mySecondPara"]').addClass('highlight');
	
	// case 2 - looking for everything except
	//$('p[name!="mySecondPara"]').addClass('highlight');
	
	// case 3 - give me everything, but "not" those with name attr
	//$('p').not('[name]').addClass('highlight');
	
	// jQuery specific selector syntax
	
	// the :header
	
	//$(':header').addClass('highlight');
	
	// the :empty
	
	//$(':empty').text('You seemed lonely so i gave you some text');
	
	//$('li:even').addClass('highlight');
});
*/

		/*
			read jQuery API documentation:
				Selectors - a list of all selectors
				
		*/